name: 'Cache'
on:
  push:
    tags:
      - 'v*'
jobs:
  cachebuild:
    name: Cache Build
    uses: ./.github/workflows/⚡reusable-build.yaml
    secrets: inherit
    with:
      atomi_platform: sulfone
      atomi_service: iridium
      bin_name: cyanprint
      push: 'yes'
