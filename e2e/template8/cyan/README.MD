# Template8

E2E test parent template that depends on template7 with preset_answers for 2/3 of its questions (database and port pre-set, enableSSL left for user). Tests the preset answers injection feature.
